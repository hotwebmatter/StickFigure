using System;
using static System.Console;

namespace StickFigure
{
    class Program
    {
        static void Main(string[] args)
        {
            // tiny stick figure
            // (because I already understand character escape sequences)
            WriteLine();
            WriteLine("\\o/");
            WriteLine(" |");
            WriteLine("/ \\");
            WriteLine();
            ReadKey();
        }
    }
}